app:
  build: .
  environment:
    DATABASE_HOST:     database
    DATABASE_PASSWORD: 'fooby'
    DATABASE_PORT:     3306
  links:
   - db:database
  volumes:
    - .:/usr/src/app

db:
  image: mysql:latest
  environment:
    MYSQL_ROOT_PASSWORD: 'fooby'
  expose:
    - "3306"
